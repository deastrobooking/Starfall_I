[package]
name = "starfall-framework-consumer"
version = "0.1.0"
edition = "2021"
description = "Minimal consumer of Starfall's public graph and platformer facade"
publish = false

[dependencies]
starfall-i = { path = "../..", default-features = false }
