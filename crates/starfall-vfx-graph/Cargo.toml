[package]
name = "starfall-vfx-graph"
version = "0.1.0"
edition = "2021"
description = "Typed graph authoring and compilation adapters for Starfall VFX systems (Niagara-lite)"
readme = "README.md"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
starfall-graph = { path = "../starfall-graph" }
