[package]
name = "starfall-graph"
version = "0.1.0"
edition = "2021"
description = "Typed graph documents and native node registration for Starfall"
publish = false

[dependencies]
bevy_app = "0.19.1"
bevy_ecs = "0.19.1"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
